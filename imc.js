var user = ["Isabella","Marcio","Calu"];
var senha = ["1234","5678","9101"];
var verificar;

function login(){
	verificar = true;
	if(usuario == user[0]){
	if (senha == senha[0]{
		window.alert("Ir para HOME");
		verificar = false;
	}
	}
	while (verificar == "true");
}
		
}